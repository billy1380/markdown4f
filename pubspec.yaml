name: markdown4f
description: A markdown library for Dart.
version: 1.0.2

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.6.1
